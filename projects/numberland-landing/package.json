{
  "name": "numberland-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Numberland — gamified digital-access landing page (Persian / RTL).",
  "scripts": {
    "build": "tailwindcss -i ./src.css -o ./tailwind.css --minify",
    "watch": "tailwindcss -i ./src.css -o ./tailwind.css --watch",
    "serve": "python3 -m http.server 8123",
    "dev": "npm run build && npm run serve",
    "start": "npm run dev"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
